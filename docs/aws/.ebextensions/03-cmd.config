packages:
    yum:
        ImageMagick: []
        ImageMagick-devel: []

commands:
    03-cmd:
        command: |
            sudo mkdir -p /var/app/staging/bootstrap/cache \
            && sudo chmod -R 777 /var/app/staging/bootstrap/cache
